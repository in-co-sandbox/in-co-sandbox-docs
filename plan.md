# GST OpenAPI Specification Migration Plan
This document outlines the changes needed to migrate GST API specifications from Readme to Mintlify format.
**Note:** The Readme specification has been corrected with proper whitespace.

## 1. Common Paths (89 paths)
The following paths exist in both Readme and Mintlify specifications. Changes from Readme need to be applied to Mintlify.

### 1. /gst/compliance/e-invoice/pdf/generate
**Description:**
- Readme: `Generate the PDF for the e-Invoice generated by passing the signed QR code and signed e-Invoice.`
- Mintlify: ``
- **Action:** Update Mintlify description

**Modified Parameters:**
- `authorization` (header):
  - required: False -> True

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['@entity', 'irn', 'signed_qr_code', 'signed_invoice'], Mintlify=[]

---

### 2. /gst/compliance/e-invoice/tax-payer/authenticate
**Summary:**
- Readme: `e-Invoice Authentication`
- Mintlify: `e-Invoice Authenticate`
- **Action:** Update Mintlify summary

**Modified Parameters:**
- `force` (query):
  - description: '' -> 'The value of this attribute is to be set true if the client needs to refresh the token 10 minutes before expiry. Possible Values - true or false'
  - required: False -> True
  - schema: needs update
- `authorization` (header):
  - description: 'e-Invoice access token' -> 'JWT access token'
- `x-source` (header):
  - description: '' -> 'Specifies the source to access, either primary (NIC 1) or secondary (NIC 2)'
  - schema: needs update
  - default: None -> primary
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['username', 'password', 'gstin'], Mintlify=[]

---

### 3. /gst/compliance/e-invoice/tax-payer/gstin/search
**Summary:**
- Readme: `Search GSTIN Details`
- Mintlify: `Search GSTIN`
- **Action:** Update Mintlify summary

**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['gstin'], Mintlify=[]

---

### 4. /gst/compliance/e-invoice/tax-payer/invoice
**Summary:**
- Readme: `Generate e-Invoice`
- Mintlify: `Generate IRN`
- **Action:** Update Mintlify summary

**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['RAW_BODY'], Mintlify=[]

---

### 5. /gst/compliance/e-invoice/tax-payer/invoice/{irn}
**Missing Parameters in Mintlify:**
- `irn` (path):
```json
{
  "name": "irn",
  "in": "path",
  "description": "Invoice Reference Number",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `x-source` (header):
  - description: '' -> 'Specifies the source to access, either primary (NIC 1) or secondary (NIC 2)'
  - schema: needs update
  - default: None -> primary
- `x-proxy` (header):
  - description: '' -> 'Enables criss-cross operations by routing requests through the specified source (e.g., access primary via secondary if primary is unavailable)'
  - schema: needs update
- `x-api-version` (header):
  - schema: needs update

---

### 6. /gst/compliance/e-invoice/tax-payer/invoice/{irn}/cancel
**Summary:**
- Readme: `Cancel e-Invoice`
- Mintlify: `Cancel IRN`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `irn` (path):
```json
{
  "name": "irn",
  "in": "path",
  "description": "Invoice Reference Number",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `x-source` (header):
  - description: '' -> 'Specifies the source to access, either primary (NIC 1) or secondary (NIC 2)'
  - schema: needs update
  - default: None -> primary
- `x-proxy` (header):
  - description: '' -> 'Enables criss-cross operations by routing requests through the specified source (e.g., access primary via secondary if primary is unavailable)'
  - schema: needs update
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['Irn', 'CnlRsn'], Mintlify=[]

---

### 7. /gst/compliance/e-invoice/tax-payer/invoice/{irn}/e-way-bill
**Missing Parameters in Mintlify:**
- `irn` (path):
```json
{
  "name": "irn",
  "in": "path",
  "description": "Invoice Reference Number",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `x-source` (header):
  - description: '' -> 'Specifies the source to access, either primary (NIC 1) or secondary (NIC 2)'
  - schema: needs update
  - default: None -> primary
- `x-proxy` (header):
  - description: '' -> 'Enables criss-cross operations by routing requests through the specified source (e.g., access primary via secondary if primary is unavailable)'
  - schema: needs update
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['Irn', 'Distance'], Mintlify=[]

---

### 8. /gst/compliance/e-way-bill/consignee/bill/{ewb_no}/reject
**Missing Parameters in Mintlify:**
- `ewb_no` (path):
```json
{
  "name": "ewb_no",
  "in": "path",
  "description": "E-Way Bill number to be rejected",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['ewbNo'], Mintlify=[]

---

### 9. /gst/compliance/e-way-bill/consignee/bills
**Summary:**
- Readme: `Get E-Way Bills By Date`
- Mintlify: `Get E-Way Bills by Date`
- **Action:** Update Mintlify summary

**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update
- `generated_date` (query):
  - description: '' -> 'E-Way bill generated date'
  - required: False -> True
  - schema: needs update

---

### 10. /gst/compliance/e-way-bill/consignor/bill
**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['RAW_BODY'], Mintlify=[]

---

### 11. /gst/compliance/e-way-bill/consignor/bill/{ewb_no}/cancel
**Missing Parameters in Mintlify:**
- `ewb_no` (path):
```json
{
  "name": "ewb_no",
  "in": "path",
  "description": "E-Way Bill number",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['ewbNo', 'cancelRsnCode'], Mintlify=[]

---

### 12. /gst/compliance/e-way-bill/consignor/bill/{ewb_no}/extend
**Missing Parameters in Mintlify:**
- `ewb_no` (path):
```json
{
  "name": "ewb_no",
  "in": "path",
  "description": "E-Way Bill number",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme

---

### 13. /gst/compliance/e-way-bill/consignor/bill/{ewb_no}/transporter
**Summary:**
- Readme: `Update Transporter`
- Mintlify: `Update Transporter Details`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `ewb_no` (path):
```json
{
  "name": "ewb_no",
  "in": "path",
  "description": "E-Way Bill number",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme

---

### 14. /gst/compliance/e-way-bill/consignor/bill/{ewb_no}/vehicle
**Missing Parameters in Mintlify:**
- `ewb_no` (path):
```json
{
  "name": "ewb_no",
  "in": "path",
  "description": "E-Way Bill number",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme

---

### 15. /gst/compliance/e-way-bill/consignor/bills
**Summary:**
- Readme: `Get E-Way Bills By Date`
- Mintlify: `Get E-Way Bills by Date`
- **Action:** Update Mintlify summary

**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update
- `generated_date` (query):
  - description: '' -> 'E-Way bill generated date'
  - required: False -> True
  - schema: needs update
- `rejected` (query):
  - description: '' -> 'Pass true to get rejected E-Way Bills'
  - schema: needs update

---

### 16. /gst/compliance/e-way-bill/tax-payer/authenticate
**Summary:**
- Readme: `E-Way Bill Authentication`
- Mintlify: `E-Way Bill Authenticate`
- **Action:** Update Mintlify summary

**Modified Parameters:**
- `authorization` (header):
  - description: 'E-Way Bill access token' -> 'JWT access token'
- `x-source` (header):
  - description: '' -> 'Source. Possible Values: primary, secondary or tertiary'
  - schema: needs update
  - default: None -> primary
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['username', 'password', 'gstin'], Mintlify=[]

---

### 17. /gst/compliance/e-way-bill/tax-payer/bill/{ewb_no}
**Missing Parameters in Mintlify:**
- `ewb_no` (path):
```json
{
  "name": "ewb_no",
  "in": "path",
  "description": "E-Way Bill number",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update

---

### 18. /gst/compliance/e-way-bill/tax-payer/error-list
**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update

---

### 19. /gst/compliance/e-way-bill/tax-payer/gstin/search
**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['gstin'], Mintlify=[]

---

### 20. /gst/compliance/e-way-bill/tax-payer/hsn
**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update
- `hsn_code` (query):
  - description: '' -> 'HSN/SAC code'
  - required: False -> True
  - schema: needs update

---

### 21. /gst/compliance/e-way-bill/tax-payer/transin/search
**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['transin'], Mintlify=[]

---

### 22. /gst/compliance/e-way-bill/transporter/bill/{ewb_no}/extend
**Missing Parameters in Mintlify:**
- `ewb_no` (path):
```json
{
  "name": "ewb_no",
  "in": "path",
  "description": "E-Way Bill number",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme

---

### 23. /gst/compliance/e-way-bill/transporter/bill/{ewb_no}/transporter
**Summary:**
- Readme: `Update Transporter`
- Mintlify: `Update Transporter Details`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `ewb_no` (path):
```json
{
  "name": "ewb_no",
  "in": "path",
  "description": "E-Way Bill number",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme

---

### 24. /gst/compliance/e-way-bill/transporter/bill/{ewb_no}/vehicle
**Missing Parameters in Mintlify:**
- `ewb_no` (path):
```json
{
  "name": "ewb_no",
  "in": "path",
  "description": "E-Way Bill number",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme

---

### 25. /gst/compliance/e-way-bill/transporter/bills
**Modified Parameters:**
- `generator_state_code` (query):
  - description: '' -> 'State code of the E-Way Bill generator'
  - required: False -> True
  - schema: needs update
- `generated_date` (query):
  - description: '' -> 'E-Way Bill generated date'
  - schema: needs update
- `x-api-version` (header):
  - schema: needs update
- `assigned_date` (query):
  - description: '' -> 'E-Way Bill assigned date'
  - schema: needs update

---

### 26. /gst/compliance/e-way-bill/transporter/bills/list
**Summary:**
- Readme: `List E-Way Bills by Generator`
- Mintlify: `List E-Way Bills by generator`
- **Action:** Update Mintlify summary

**Modified Parameters:**
- `generated_date` (query):
  - description: '' -> 'E-Way Bill generated date'
  - schema: needs update
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['generator_gstin'], Mintlify=[]

---

### 27. /gst/compliance/public/gstin/search
**Description:**
- Readme: `Verify GST-registered businesses by passing their GSTIN.`
- Mintlify: `Returns GSTIN entity for given GSTIN`
- **Action:** Update Mintlify description

**Modified Parameters:**
- `authorization` (header):
  - schema: needs update
- `x-api-key` (header):
  - schema: needs update
- `x-accept-cache` (header):
  - schema: needs update
- `x-api-version` (header):
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['gstin'], Mintlify=[]

---

### 28. /gst/compliance/public/gstrs/track
**Description:**
- Readme: `Check compliance for a GST-registered business by passing the financial year and the GSTIN. Further, you can filter the list by passing the return type.`
- Mintlify: `This API will be used to view e-filed returns details.`
- **Action:** Update Mintlify description

**Modified Parameters:**
- `financial_year` (query):
  - description: '' -> 'Financial Year of GST Returns e.g. FY 2021-22'
  - required: False -> True
- `gstr` (query):
  - description: '' -> 'Optional GST Return type to be passed as filter. E.g. gstr-1'

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['gstin'], Mintlify=[]

---

### 29. /gst/compliance/public/pan/search
**Description:**
- Readme: `Verify a business registered under GST by passing the PAN and state code.`
- Mintlify: `Returns GSTIN entity for given GSTIN`
- **Action:** Update Mintlify description

**Modified Parameters:**
- `state_code` (query):
  - description: '' -> 'GST state code'
  - required: False -> True

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['pan'], Mintlify=[]

---

### 30. /gst/compliance/tax-payer/e-invoice/{irn}
**Missing Parameters in Mintlify:**
- `irn` (path):
```json
{
  "name": "irn",
  "in": "path",
  "description": "Invoice Reference Number",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update

---

### 31. /gst/compliance/tax-payer/e-invoices/{year}/{month}/purchases
**Summary:**
- Readme: `Submit Job`
- Mintlify: `List Purchase e-Invoices Job`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year. E.g. 2024",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month. E.g. 04",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `supply_type` (query):
  - description: '' -> 'Supply type. E.g. B2B, SEZWP, SEZWOP, DEXP, EXPWP, EXPWOP'
  - required: False -> True

---

### 32. /gst/compliance/tax-payer/e-invoices/{year}/{month}/sales
**Summary:**
- Readme: `Submit Job`
- Mintlify: `List Sales e-Invoices Job`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year. E.g. 2024",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month. E.g. 04",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `supply_type` (query):
  - description: '' -> 'Supply type. E.g. B2B, SEZWP, SEZWOP, DEXP, EXPWP, EXPWOP'
  - required: False -> True

---

### 33. /gst/compliance/tax-payer/evc/otp
**Modified Parameters:**
- `gstr` (query):
  - description: '' -> 'GSTR Type. Example: gstr-1'
  - required: False -> True

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['pan'], Mintlify=[]

---

### 34. /gst/compliance/tax-payer/gstrs/gstr-1/at/{year}/{month}
**Summary:**
- Readme: `GSTR-1 AT`
- Mintlify: `Get GSTR-1 AT`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

---

### 35. /gst/compliance/tax-payer/gstrs/gstr-1/ata/{year}/{month}
**Summary:**
- Readme: `GSTR-1 ATA`
- Mintlify: `Get GSTR-1 ATA`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

---

### 36. /gst/compliance/tax-payer/gstrs/gstr-1/b2b/{year}/{month}
**Summary:**
- Readme: `GSTR-1 B2B`
- Mintlify: `Get GSTR-1 B2B`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `counterparty_gstin` (query):
  - description: '`optional` Counter party GSTIN' -> 'Counter party GSTIN'
- `action_required` (query):
  - description: '`optional` Status filter for filtering the invoices. Possible values: `Y` or `N`. Status filter for filtering the invoices. Use filter value Y to fetches the invoices where tax payer need to take action. Already accepted or uploaded invoices can be fetched by using filter value N.' -> 'Filter the invoices by status. Possible values: Y - Action required, N - Accepted/uploaded invoices'
- `from` (query):
  - description: '`optional` From Date (DD/MM/YYYY)' -> 'From Date (DD/MM/YYYY)'

---

### 37. /gst/compliance/tax-payer/gstrs/gstr-1/b2ba/{year}/{month}
**Summary:**
- Readme: `GSTR-1 B2BA`
- Mintlify: `Get GSTR-1 B2BA`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `counterparty_gstin` (query):
  - description: '`optional` Counter party GSTIN' -> 'Counter party GSTIN'
- `action_required` (query):
  - description: '`optional` Status filter for filtering the invoices. Possible values: `Y` or `N`. Status filter for filtering the invoices. Use filter value Y to fetches the invoices where tax payer need to take action. Already accepted or uploaded invoices can be fetched by using filter value N.' -> 'Filter the invoices by status. Possible values: Y - Action required, N - Accepted/uploaded invoices'
- `from` (query):
  - description: '`optional` From Date (DD/MM/YYYY)' -> 'From Date (DD/MM/YYYY)'

---

### 38. /gst/compliance/tax-payer/gstrs/gstr-1/b2cl/{year}/{month}
**Summary:**
- Readme: `GSTR-1 B2CL`
- Mintlify: `Get GSTR-1 B2CL`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `state_code` (query):
  - description: '`optional` State code' -> 'State code'

---

### 39. /gst/compliance/tax-payer/gstrs/gstr-1/b2cla/{year}/{month}
**Summary:**
- Readme: `GSTR-1 B2CLA`
- Mintlify: `Get GSTR-1 B2CLA`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `state_code` (query):
  - description: '`optional` State code' -> 'State code'

---

### 40. /gst/compliance/tax-payer/gstrs/gstr-1/b2cs/{year}/{month}
**Summary:**
- Readme: `GSTR-1 B2CS`
- Mintlify: `Get GSTR-1 B2CS`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

---

### 41. /gst/compliance/tax-payer/gstrs/gstr-1/b2csa/{year}/{month}
**Summary:**
- Readme: `GSTR-1 B2CSA`
- Mintlify: `Get GSTR-1 B2CSA`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

---

### 42. /gst/compliance/tax-payer/gstrs/gstr-1/cdnr/{year}/{month}
**Summary:**
- Readme: `GSTR-1 CDNR`
- Mintlify: `Get GSTR-1 CDNR`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `action_required` (query):
  - description: '`optional` Status filter for filtering the invoices. Possible values: `Y` or `N`. Status filter for filtering the invoices. Use filter value Y to fetches the invoices where tax payer need to take action. Already accepted or uploaded invoices can be fetched by using filter value N.' -> 'Filter the notes by status. Possible values: Y - Action required, N - Accepted/uploaded notes'
- `from` (query):
  - description: '`optional` From Date (DD/MM/YYYY)' -> 'From Date (DD/MM/YYYY)'

---

### 43. /gst/compliance/tax-payer/gstrs/gstr-1/cdnra/{year}/{month}
**Summary:**
- Readme: `GSTR-1 CDNRA`
- Mintlify: `Get GSTR-1 CDNRA`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `action_required` (query):
  - description: '`optional` Status filter for filtering the invoices. Possible values: `Y` or `N`. Status filter for filtering the invoices. Use filter value Y to fetches the invoices where tax payer need to take action. Already accepted or uploaded invoices can be fetched by using filter value N.' -> 'Filter the notes by status. Possible values: Y - Action required, N - Accepted/uploaded notes'
- `from` (query):
  - description: '`optional` From Date (DD/MM/YYYY)' -> 'From Date (DD/MM/YYYY)'

---

### 44. /gst/compliance/tax-payer/gstrs/gstr-1/cdnur/{year}/{month}
**Summary:**
- Readme: `GSTR-1 CDNUR`
- Mintlify: `Get GSTR-1 CDNUR`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

---

### 45. /gst/compliance/tax-payer/gstrs/gstr-1/cdnura/{year}/{month}
**Summary:**
- Readme: `GSTR-1 CDNURA`
- Mintlify: `Get GSTR-1 CDNURA`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

---

### 46. /gst/compliance/tax-payer/gstrs/gstr-1/doc-issue/{year}/{month}
**Summary:**
- Readme: `GSTR-1 Document Issued`
- Mintlify: `Get GSTR-1 Document Issued`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

---

### 47. /gst/compliance/tax-payer/gstrs/gstr-1/exp/{year}/{month}
**Summary:**
- Readme: `GSTR-1 EXP`
- Mintlify: `Get GSTR-1 EXP`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

---

### 48. /gst/compliance/tax-payer/gstrs/gstr-1/expa/{year}/{month}
**Summary:**
- Readme: `GSTR-1 NIL Supplies`
- Mintlify: `Get GSTR-1 EXPA`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

---

### 49. /gst/compliance/tax-payer/gstrs/gstr-1/hsn/{year}/{month}
**Summary:**
- Readme: `GSTR-1 HSN Summary`
- Mintlify: `Get GSTR-1 HSN Summary`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

---

### 50. /gst/compliance/tax-payer/gstrs/gstr-1/{year}/{month}
**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['fp', 'gstin', 'gt', 'cur_gt'], Mintlify=[]

---

### 51. /gst/compliance/tax-payer/gstrs/gstr-1/{year}/{month}/file
**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `pan` (query):
  - description: '`required` PAN number of authorized signatory' -> 'PAN of the authorized signatory'
  - required: False -> True
- `otp` (query):
  - description: '`required` Generated EVC' -> 'Generated EVC OTP'
  - required: False -> True
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['RAW_BODY'], Mintlify=[]

---

### 52. /gst/compliance/tax-payer/gstrs/gstr-1/{year}/{month}/reset
**Modified Parameters:**
- `x-api-version` (header):
  - required: False -> True

**Request Body Changes:**
- Request body schema differs - needs update from Readme

---

### 53. /gst/compliance/tax-payer/gstrs/gstr-2a/amdhist/{year}/{month}
**Summary:**
- Readme: `GSTR-2A AMDHIST`
- Mintlify: `GetGSTR-2A AMDHIST`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `port_code` (query):
  - required: False -> True
- `bill_of_entry_number` (query):
  - required: False -> True
  - schema: needs update
- `bill_of_entry_date` (query):
  - required: False -> True
  - schema: needs update

---

### 54. /gst/compliance/tax-payer/gstrs/gstr-2a/b2b/{year}/{month}
**Summary:**
- Readme: `GSTR-2A B2B`
- Mintlify: `Get GSTR-2A B2B`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `counterparty_gstin` (query):
  - description: '`optional` Counter party GSTIN' -> 'Filter by supplier / counterparty GSTIN'
- `from` (query):
  - schema: needs update

---

### 55. /gst/compliance/tax-payer/gstrs/gstr-2a/cdn/{year}/{month}
**Summary:**
- Readme: `GSTR-2A CDN`
- Mintlify: `Get GSTR-2A CDN`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `counterparty_gstin` (query):
  - description: '`optional` Counter party GSTIN' -> 'Filter by supplier / counterparty GSTIN'
- `from` (query):
  - schema: needs update

---

### 56. /gst/compliance/tax-payer/gstrs/gstr-2a/cdna/{year}/{month}
**Summary:**
- Readme: `GSTR-2A CDNA`
- Mintlify: `Get GSTR-2A CDNA`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `counterparty_gstin` (query):
  - description: '`optional` Counter party GSTIN' -> 'Filter by supplier / counterparty GSTIN'

---

### 57. /gst/compliance/tax-payer/gstrs/gstr-2a/impg/{year}/{month}
**Summary:**
- Readme: `GSTR-2A IMPG`
- Mintlify: `Get GSTR-2A IMPG`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `from` (query):
  - schema: needs update

---

### 58. /gst/compliance/tax-payer/gstrs/gstr-2a/impgsez/{year}/{month}
**Summary:**
- Readme: `GSTR-2A IMPGSEZ`
- Mintlify: `Get GSTR-2A IMPGSEZ`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `from` (query):
  - schema: needs update

---

### 59. /gst/compliance/tax-payer/gstrs/gstr-2a/isd/{year}/{month}
**Summary:**
- Readme: `GSTR-2A ISD`
- Mintlify: `Get GSTR-2A ISD`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `counterparty_gstin` (query):
  - description: '`optional` Counter party GSTIN' -> 'Counter party GSTIN'

---

### 60. /gst/compliance/tax-payer/gstrs/gstr-2a/tcs/{year}/{month}
**Summary:**
- Readme: `GSTR-2A TCS`
- Mintlify: `Get GSTR-2A TCS`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

---

### 61. /gst/compliance/tax-payer/gstrs/gstr-2a/tds/{year}/{month}
**Summary:**
- Readme: `GSTR-2A TDS`
- Mintlify: `Get GSTR-2A TDS`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

---

### 62. /gst/compliance/tax-payer/gstrs/gstr-2a/{year}/{month}
**Summary:**
- Readme: `GSTR-2A Document`
- Mintlify: `Get GSTR-2A`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

---

### 63. /gst/compliance/tax-payer/gstrs/gstr-2b/regenerate
**Summary:**
- Readme: `GSTR-2B Regeneration Status`
- Mintlify: `Fetch GSTR-2B Regeneration Status`
- **Action:** Update Mintlify summary

**Description:**
- Readme: `API to get the status of GSTR-2B regeneration from GST Network.`
- Mintlify: ``
- **Action:** Update Mintlify description

**Modified Parameters:**
- `reference_id` (query):
  - description: '' -> 'Reference id received from Regenerate GSTR-2B API'
  - required: False -> True

---

### 64. /gst/compliance/tax-payer/gstrs/gstr-2b/{year}/{month}
**Summary:**
- Readme: `GSTR-2B Document`
- Mintlify: `Get GSTR-2B Document`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `file_number` (query):
  - description: ''optional' eg. 2 To be sent when there are multiple files' -> 'Specify file_number incrementally (from 1 to fc) in additional requests to retrieve all invoice data. Only applicable if status_cd = 3.'
  - schema: needs update
- `x-api-version` (header):
  - schema: needs update
  - default: None -> 1.0

---

### 65. /gst/compliance/tax-payer/gstrs/gstr-2b/{year}/{month}/regenerate
**Description:**
- Readme: `Seamlessly regenerate GSTR-2B on-demand with this API. Whenever actions are taken on invoices within the Invoice Management System (IMS), GSTR-2B needs to be regenerated per the latest invoice status (Accepted/Rejected/Pending).`
- Mintlify: ``
- **Action:** Update Mintlify description

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `x-api-version` (header):
  - schema: needs update
  - default: None -> 1.0

---

### 66. /gst/compliance/tax-payer/gstrs/gstr-3b/{year}/{month}
**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['RAW_BODY'], Mintlify=[]

---

### 67. /gst/compliance/tax-payer/gstrs/gstr-3b/{year}/{month}/file
**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `pan` (query):
  - description: '' -> 'PAN of the authorized signatory'
  - required: False -> True
- `otp` (query):
  - description: '' -> 'Generated EVC OTP'
  - required: False -> True
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['RAW_BODY'], Mintlify=[]

---

### 68. /gst/compliance/tax-payer/gstrs/gstr-3b/{year}/{month}/offset-liability
**Summary:**
- Readme: `Offset Liability`
- Mintlify: `GSTR-3B Offset Liability`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['RAW_BODY'], Mintlify=[]

---

### 69. /gst/compliance/tax-payer/gstrs/gstr-9
**Modified Parameters:**
- `financial_year` (query):
  - description: '' -> 'Financial year. E.g., FY 2023-24'
  - required: False -> True

---

### 70. /gst/compliance/tax-payer/gstrs/gstr-9/auto-calculated
**Modified Parameters:**
- `financial_year` (query):
  - description: '' -> 'Financial year. E.g., FY 2023-24'
  - required: False -> True

---

### 71. /gst/compliance/tax-payer/gstrs/gstr-9/file
**Modified Parameters:**
- `financial_year` (query):
  - description: '' -> 'Financial year. E.g., FY 2023-24'
  - required: False -> True
- `pan` (query):
  - description: '' -> 'PAN of the authorized signatory'
  - required: False -> True
- `otp` (query):
  - description: '`required` Generated EVC' -> 'Generated EVC OTP'
  - required: False -> True
  - schema: needs update

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['RAW_BODY'], Mintlify=[]

---

### 72. /gst/compliance/tax-payer/gstrs/gstr-9/save
**Modified Parameters:**
- `financial_year` (query):
  - description: '' -> 'Financial year. E.g., FY 2023-24'
  - required: False -> True

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['RAW_BODY'], Mintlify=[]

---

### 73. /gst/compliance/tax-payer/gstrs/gstr-9/table-8a
**Summary:**
- Readme: `Section 8A Details`
- Mintlify: `GSTR-9 Section 8A Details`
- **Action:** Update Mintlify summary

**Modified Parameters:**
- `financial_year` (query):
  - description: '' -> 'Financial year. E.g., FY 2023-24'
  - required: False -> True

---

### 74. /gst/compliance/tax-payer/gstrs/{gstr}/{year}/{month}/new-proceed
**Summary:**
- Readme: `New Proceed to File`
- Mintlify: `New Proceed to File GSTR`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `gstr` (path):
```json
{
  "name": "gstr",
  "in": "path",
  "description": "Return Type",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year for which Return is to be filled",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month for which Retrun is to be filled",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `x-api-version` (header):
  - required: False -> True
- `is_nil` (query):
  - description: '' -> 'For proceeding to file nil report Possible values: Y or N'

**Request Body Changes:**
- Request body schema differs - needs update from Readme

---

### 75. /gst/compliance/tax-payer/gstrs/{gstr}/{year}/{month}/proceed
**Summary:**
- Readme: `Proceed to file`
- Mintlify: `Proceed to File GSTR`
- **Action:** Update Mintlify summary

**Description:**
- Readme: `Proceed to file marks the return ready for filing. Post this EVC OTP can be generated for filing.`
- Mintlify: `Authenticated tax payer can save entire GSTR-1 invoices by uploading json to GST portal`
- **Action:** Update Mintlify description

**Missing Parameters in Mintlify:**
- `gstr` (path):
```json
{
  "name": "gstr",
  "in": "path",
  "description": "Return Type",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year for which Return is to be filled",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month for which Retrun is to be filled",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `x-api-version` (header):
  - required: False -> True

**Request Body Changes:**
- Request body schema differs - needs update from Readme

---

### 76. /gst/compliance/tax-payer/gstrs/{year}/{month}/status
**Summary:**
- Readme: `GST Return status`
- Mintlify: `Get GST Return Status`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

**Modified Parameters:**
- `reference_id` (query):
  - description: '`required` Reference ID' -> 'Reference id'
  - required: False -> True

---

### 77. /gst/compliance/tax-payer/invoices
**Modified Parameters:**
- `section` (query):
  - description: 'E.g. B2B, B2BA, CN, etc.' -> 'Section of the transaction. Possible values: B2B, B2BA, CN, CNA, DN, DNA, ECOM, ECOMA'
  - required: False -> True
  - schema: needs update
  - default: None -> b2b
- `status` (query):
  - description: 'Optional. Get invoices for a specific status. E.g. accepted, rejected, pending, no_action_taken' -> 'Optional filter to fetch invoices of specific status. Possible values: accepted, rejected, pending, no_action_taken'

---

### 78. /gst/compliance/tax-payer/invoices/count
**Modified Parameters:**
- `type_of_goods` (query):
  - description: 'E.g. imports, inward_supplies and others' -> 'Type of goods. E.g. imports, inward_supplies, others'
  - required: False -> True
  - schema: needs update
  - default: None -> others

---

### 79. /gst/compliance/tax-payer/invoices/status
**Modified Parameters:**
- `reference_id` (query):
  - description: 'E.g. Reference idon taking Save or Reset action' -> 'Pass the reference id on taking Save or Reset action'
  - required: False -> True

---

### 80. /gst/compliance/tax-payer/invoices/status/reset
**Request Body Changes:**
- Request body schema differs - needs update from Readme

---

### 81. /gst/compliance/tax-payer/invoices/status/save
**Request Body Changes:**
- Request body schema differs - needs update from Readme

---

### 82. /gst/compliance/tax-payer/ledgers/bal/{year}/{month}
**Summary:**
- Readme: `Cash ITC Balance`
- Mintlify: `Get Cash ITC Balance`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `year` (path):
```json
{
  "name": "year",
  "in": "path",
  "description": "Year e.g. 2020",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

- `month` (path):
```json
{
  "name": "month",
  "in": "path",
  "description": "Month e.g. 01",
  "schema": {
    "type": "string"
  },
  "required": true
}
```

---

### 83. /gst/compliance/tax-payer/ledgers/cash
**Summary:**
- Readme: `Cash Ledger`
- Mintlify: `Get Cash Ledger`
- **Action:** Update Mintlify summary

**Modified Parameters:**
- `from` (query):
  - description: '`required` From Date (DD/MM/YYYY)' -> 'From Date (DD/MM/YYYY)'
- `to` (query):
  - description: '`required` To Date (DD/MM/YYYY)' -> 'To Date (DD/MM/YYYY)'

---

### 84. /gst/compliance/tax-payer/ledgers/itc
**Summary:**
- Readme: `ITC Ledger`
- Mintlify: `Get ITC Ledger`
- **Action:** Update Mintlify summary

**Modified Parameters:**
- `from` (query):
  - description: '`required` From Date (DD/MM/YYYY)' -> 'From Date (DD/MM/YYYY)'
  - required: False -> True
  - schema: needs update
- `to` (query):
  - description: '`required` To Date (DD/MM/YYYY)' -> 'To Date (DD/MM/YYYY)'
  - required: False -> True
  - schema: needs update

---

### 85. /gst/compliance/tax-payer/ledgers/tax/{year}/{month}
**Summary:**
- Readme: `Return Liability Ledger`
- Mintlify: `Get Return Liability Ledger`
- **Action:** Update Mintlify summary

**Missing Parameters in Mintlify:**
- `from` (query):
```json
{
  "name": "from",
  "in": "query",
  "description": "From Date (DD/MM/YYYY)",
  "required": true,
  "schema": {
    "type": "string"
  }
}
```

- `to` (query):
```json
{
  "name": "to",
  "in": "query",
  "description": "To Date (DD/MM/YYYY)",
  "required": true,
  "schema": {
    "type": "string"
  }
}
```

---

### 86. /gst/compliance/tax-payer/logout
**Status:** No significant differences detected (may need manual review)

### 87. /gst/compliance/tax-payer/otp
**Modified Parameters:**
- `authorization` (header):
  - description: 'Taxpayer access token' -> 'JWT access token'
- `x-source` (header):
  - description: '' -> 'Source. Possible Values: primary (default) or secondary'
  - schema: needs update
  - default: None -> primary

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['username', 'gstin'], Mintlify=[]

---

### 88. /gst/compliance/tax-payer/otp/verify
**Modified Parameters:**
- `otp` (query):
  - description: '' -> 'One time password'
  - required: False -> True
  - schema: needs update
- `authorization` (header):
  - description: 'Taxpayer access token' -> 'JWT access token'
- `x-source` (header):
  - description: '' -> 'Source. Possible Values: primary (default) or secondary'
  - schema: needs update
  - default: None -> primary

**Request Body Changes:**
- Request body schema differs - needs update from Readme
- Required fields differ: Readme=['username', 'gstin'], Mintlify=[]

---

### 89. /gst/compliance/tax-payer/session/refresh
**Summary:**
- Readme: `Refresh Access Token`
- Mintlify: `Refresh Session`
- **Action:** Update Mintlify summary

---

## 2. Paths Only in Readme (2 paths)
These paths exist in Readme but not in Mintlify. Consider adding them to Mintlify.

1. `/gst/compliance/tax-payer/ledgers/other`
2. `/gst/compliance/tax-payer/ledgers/{gstr}/liability/{year}/{month}`

## 3. Paths Only in Mintlify (22 paths)
These paths exist in Mintlify but not in Readme. These are likely newer endpoints.

1. `/gst/compliance/e-way-bill/consignor/bill/{ewb_no}/multi-vehicle`
2. `/gst/compliance/e-way-bill/consignor/bill/{ewb_no}/multi-vehicle/vehicle`
3. `/gst/compliance/e-way-bill/consignor/consolidated-bill`
4. `/gst/compliance/e-way-bill/consignor/consolidated-bill/{consolidated_ewb_no}`
5. `/gst/compliance/e-way-bill/consignor/consolidated-bill/{consolidated_ewb_no}/regenerate`
6. `/gst/compliance/e-way-bill/transporter/bill/{ewb_no}/multi-vehicle`
7. `/gst/compliance/e-way-bill/transporter/bill/{ewb_no}/multi-vehicle/vehicle`
8. `/gst/compliance/e-way-bill/transporter/consolidated-bill`
9. `/gst/compliance/e-way-bill/transporter/consolidated-bill/{consolidated_ewb_no}`
10. `/gst/compliance/e-way-bill/transporter/consolidated-bill/{consolidated_ewb_no}/regenerate`
11. `/gst/compliance/tax-payer/gstrs/file-details/download`
12. `/gst/compliance/tax-payer/gstrs/gstr-1/nil/{year}/{month}`
13. `/gst/compliance/tax-payer/gstrs/gstr-2a/b2ba/{year}/{month}`
14. `/gst/compliance/tax-payer/gstrs/gstr-3b/{year}/{month}/auto-liability-calc`
15. `/gst/compliance/tax-payer/gstrs/gstr-3b/{year}/{month}/validate`
16. `/gst/compliance/tax-payer/gstrs/gstr-4-annual/in-out-supply`
17. `/gst/compliance/tax-payer/gstrs/gstr-4-annual/summary`
18. `/gst/compliance/tax-payer/gstrs/gstr-4-annual/tdscmp-summary`
19. `/gst/compliance/tax-payer/gstrs/gstr-4-annual/{year}/{month}`
20. `/gst/compliance/tax-payer/gstrs/gstr-4-annual/{year}/{month}/proceed`
21. `/gst/compliance/tax-payer/gstrs/preference`
22. `/gst/compliance/tax-payer/gstrs/{year}/{month}/file-details/download`
