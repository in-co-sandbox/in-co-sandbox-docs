---
title: Job-Based API Workflow
description: Understand how Sandbox job-based APIs work using a step-by-step workflow covering job submission, payload upload, and job polling.
---
## Overview

Several Sandbox APIs follow an **asynchronous, job-based execution model** to handle large input payloads and long-running processing reliably.

Instead of sending the full input data directly in the API request, these APIs break execution into a structured workflow:

1. A **JOB** is created to initialize processing  
2. The input payload is uploaded separately using a secure, pre-signed URL  
3. The JOB is polled until processing completes and the output is ready

This approach improves scalability, fault tolerance, and performance, while giving you clear visibility into the processing lifecycle.

The sections below walk through each step of this JOB-based workflow in detail.

<Job>
</Job>

<Steps>
  <Step title="Submit the Job" stepNumber={1} titleSize="h2">
    To begin processing, submit a **JOB** using a `POST` request to the workload-specific endpoint.

    This step initializes the asynchronous workflow and returns:
    - A unique **job_id**
    - A **pre-signed S3 URL** for uploading the input payload

    <Accordion title="Endpoint Pattern" defaultOpen>
      ```http
      POST /:workload/:function/:sub_function
      ```
    </Accordion>

    <Accordion title="Example Response">
      ```json
      {
    "code": 200,
    "timestamp": 1767788389433,
    "data": {
        "created_at": 1767788396261,
        "updated_at": 1767788396261,
        "@entity": "in.co.sandbox.it.calculator.tax_pnl.securities.job",
        "security_type": "domestic",
        "url": "https://in-co-sbox-it-calc-tax-pnl-sec-dev.s3.ap-south-1.amazonaws.com/domestic/11734cbd-8f18-45ea-9eab-0717a8b3c513/workbook.input?X-Amz-Algorithm=AWS4-HMAC-SHA256&X-Amz-Content-Sha256=UNSIGNED-PAYLOAD&X-Amz-Credential=AKIARB4EOKMVLGPKU5NY%2F20260107%2Fap-south-1%2Fs3%2Faws4_request&X-Amz-Date=20260107T121956Z&X-Amz-Expires=86400&X-Amz-Signature=5f102e58f90f284c7ad265833c491cacfa5cbe22bfff7035132742e09d848387&X-Amz-SignedHeaders=host&x-amz-checksum-crc32=AAAAAA%3D%3D&x-amz-sdk-checksum-algorithm=CRC32&x-id=PutObject",
        "status": "created",
        "job_id": "11734cbd-8f18-45ea-9eab-0717a8b3c513"
    },
    "transaction_id": "11734cbd-8f18-45ea-9eab-0717a8b3c513"
}
      ```
    </Accordion>

    <ParamField body="id" type="string" required>
      Unique identifier for the JOB. This value is used to upload input data and to poll job status.
    </ParamField>

    <ParamField body="url" type="string" required>
      Pre-signed AWS S3 URL used to upload the input payload in the next step.
    </ParamField>

    <ParamField body="status" type="string" required>
      Initial state of the JOB. At creation, this will always be <code>created</code>.
    </ParamField>
  </Step>
</Steps>
<Steps>
  <Step title="Upload the Input Payload" stepNumber={2} titleSize="h2">
    After submitting the JOB, upload your input payload directly to the **pre-signed S3 URL** returned in Step 1.

    This step uses a **plain HTTP `PUT` request** and does **not** interact with Sandbox APIs.

    <Accordion title="Important Notes" defaultOpen>
      - The upload URL is an **AWS S3 pre-signed URL**
      - It is **not** a Sandbox API endpoint
      - The URL is time-bound and expires automatically
      - Do **not** modify the URL in any way
      - Do **not** send Sandbox API keys or authorization headers
    </Accordion>

    <Accordion title="Request Requirements">
      - **Method:** `PUT`  
      - **Headers:** `Content-Type`  
      - **Body:** Input payload (JSON, CSV, ZIP, etc.)  
      - **No authorization headers**
    </Accordion>

    <Accordion title="cURL Example" defaultOpen>
      ```bash
      curl --location --request PUT 'https://in-co-sandbox-it-calculator-pnl-crypto-dev.s3.ap-south-1.amazonaws.com/00f1274b-661d-49a8-bbb0-47ed6c554c40/workbook.input?X-Amz-Algorithm=AWS4-HMAC-SHA256&X-Amz-Content-Sha256=UNSIGNED-PAYLOAD&X-Amz-Credential=AKIARB4EOKMVKCZZT7XQ%2F20260107%2Fap-south-1%2Fs3%2Faws4_request&X-Amz-Date=20260107T122604Z&X-Amz-Expires=86400&X-Amz-Signature=5044812e4735aa4978f5f14d291728ecf09e48525c021d08fb1b0de8f32af830&X-Amz-SignedHeaders=host&x-amz-checksum-crc32=AAAAAA%3D%3D&x-amz-sdk-checksum-algorithm=CRC32&x-id=PutObject' \
--data ''
      ```
    </Accordion>

    A successful upload returns:

    ```text
    200 OK
    ```

    Once the payload is uploaded successfully, the JOB automatically begins processing.
  </Step>
</Steps>
<Steps>
  <Step title="Poll the Job Status" stepNumber={3} titleSize="h2">
    After the input payload is uploaded, the JOB is processed asynchronously.

    To track progress and retrieve the result, poll the **same endpoint used in Step 1**, but using a `GET` request with the `job_id`.

    <Accordion title="Endpoint Pattern" defaultOpen>
      ```http
      GET /:workload/:function/:sub_function/:job_id
      ```
    </Accordion>

    <Accordion title="Example Response — In Progress">
      ```json
      {
    "code": 200,
    "timestamp": 1767788517749,
    "data": {
        "created_at": 1767788396261,
        "updated_at": 1767788396261,
        "@entity": "in.co.sandbox.it.calculator.tax_pnl.securities.job",
        "security_type": "domestic",
        "status": "in_progress",
        "job_id": "11734cbd-8f18-45ea-9eab-0717a8b3c513"
    },
    "transaction_id": "1f077f3e-524f-4f96-942a-a0daf7af4f5e"
}
      ```
    </Accordion>

    <Accordion title="Job Statuses">
      - `created` — Job created and awaiting upload  
      - `queued` — Job queued for processing  
      - `in_progress` — Job is being processed  
      - `succeeded` — Processing complete and output available  
      - `failed` — Job failed during processing
    </Accordion>

    <Accordion title="Example Response — Succeeded">
      ```json
      {
    "code": 200,
    "timestamp": 1767788721062,
    "data": {
        "id": "02f12d0c-e7e0-4021-943c-efcc827e6ea9",
        "created_at": 1767788696635,
        "updated_at": 1767788696635,
        "@entity": "in.co.sandbox.it.calculator.pnl.crypto.job",
        "url": "https://in-co-sandbox-it-calculator-pnl-crypto-dev.s3.ap-south-1.amazonaws.com/02f12d0c-e7e0-4021-943c-efcc827e6ea9/workbook.output?X-Amz-Algorithm=AWS4-HMAC-SHA256&X-Amz-Content-Sha256=UNSIGNED-PAYLOAD&X-Amz-Credential=AKIARB4EOKMVKCZZT7XQ%2F20260107%2Fap-south-1%2Fs3%2Faws4_request&X-Amz-Date=20260107T122521Z&X-Amz-Expires=86400&X-Amz-Signature=6e59f920bda6cf995d29e5a541f05790b142f11ca6ce0782e7fd12ea6c28861c&X-Amz-SignedHeaders=host&x-amz-checksum-mode=ENABLED&x-id=GetObject",
        "status": "succeeded"
    },
    "transaction_id": "65b8a350-646b-467d-a980-01a38233d2f1"
}
      ```
    </Accordion>

    When the JOB status is `succeeded`, use the `output_url` to download the processed result.
  </Step>
</Steps>
